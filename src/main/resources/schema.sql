CREATE TABLE PHOTO (
id INTEGER AUTO_INCREMENT PRIMARY KEY,
name VARCHAR(30) NOT NULL,
description VARCHAR(64) NOT NULL
);

INSERT INTO photo ( name, description) VALUES
('ziema', 'saltaZiema');
